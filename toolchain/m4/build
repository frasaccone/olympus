#/bin/sh

set -eu
umask 022

ARCH=${ARCH:-$(uname -m)}
GMAKE=${GMAKE:-gmake}
GZ=${GZ:-gz}

"$GZ" -cd m4.tar.gz | tar -x

cd    m4*/
mkdir build
cd    build

MAKE=$GMAKE \
../configure --prefix=/ \
            --host="$ARCH-linux-musl"

"$GMAKE"
"$GMAKE" DESTDIR="$PREFIX" install
